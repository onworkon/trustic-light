# 보안 안내

API 키, 실제 페이지 본문, 개인정보가 포함된 로그는 공개 이슈나 PR에 첨부하지 마세요. 재현 예시는 가짜 데이터로 작성합니다.

키가 포함된 파일을 잘못 공유했다면 해당 서비스에서 키를 폐기·재발급하세요. 파일을 지우거나 `.gitignore`에 추가하는 것만으로 이미 만들어진 커밋이나 공개 사본의 키를 회수할 수 없습니다.

이 저장소의 `npm run check:repo`는 소스 허용 목록과 Git 인덱스의 알려진 자격 증명 형식을 검사합니다. 원본 작업 폴더에만 있을 수 있는 `private/`, `.archive/`, `.local/`과 빌드 결과는 커밋하지 않습니다. 키는 설치된 확장 프로그램의 설정 화면에서 사용자별로 등록합니다.

취약점 제보는 저장소의 **Security → Report a vulnerability**가 활성화되어 있으면 해당 비공개 경로를 사용하세요. 제공되지 않으면 Chrome 웹 스토어에 게시된 개발자 연락처를 이용하세요. 구체적인 공격 방법이나 실제 키를 공개 이슈로 게시하지 마세요.
