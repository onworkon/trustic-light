# 개발 작업 안내

Node.js 22와 Git을 사용합니다. 새 환경에서는 `npm ci`로 잠금 파일에 기록된 개발 의존성을 설치합니다. 테스트는 모의 API 응답을 사용하므로 실제 API 키를 등록할 필요가 없습니다.

```powershell
npm ci
npm run check
```

기능 수정은 `src/`, 회귀 테스트는 `tests/`에서 진행합니다. `dist/`는 생성물이므로 직접 수정하거나 커밋하지 않습니다. 수정 후 `npm run format`과 `npm run check`를 실행합니다. 웹페이지에서 확인하려면 빌드한 `dist/`를 Chrome에 로드합니다.

커밋할 파일을 스테이징한 다음 `npm run check:repo`로 Git 인덱스까지 확인합니다. 이 검사는 비공개 디렉터리, 허용되지 않은 파일, 알려진 키 형식의 포함을 차단하지만 모든 비밀 정보의 탐지를 보장하지는 않습니다. `git diff --cached`도 직접 확인합니다.

새 최상위 파일이나 소스 디렉터리를 추가하면 `scripts/source-files.mjs`의 허용 목록을 함께 수정합니다. 수정한 허용 목록은 소스 ZIP과 업로드 검사에 공통 적용됩니다. 실행 권한이 필요한 파일을 도입할 때는 Git 파일 모드 검사도 명시적으로 검토합니다.

PR에는 해결한 문제, 달라진 동작, 검증 결과를 적습니다. API 권한이나 데이터 전송 방식이 바뀌면 개인정보 처리방침과 제품 내 안내도 함께 갱신합니다.
